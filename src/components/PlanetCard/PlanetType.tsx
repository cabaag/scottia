export type Planet = {
  climate: string;
  created: string;
  diameter: number;
  edited: string;
  films: string[];
  gravity: string;
  name: string;
  // eslint-disable-next-line camelcase
  orbital_period: number;
  population: string;
  residents: string[];
  // eslint-disable-next-line camelcase
  rotation_period: number;
  // eslint-disable-next-line camelcase
  surface_water: number;
  terrain: string;
  url: string;
};
